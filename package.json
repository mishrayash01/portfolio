{
  "name": "mocha-app-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix code",
    "build": "npm run build --prefix code",
    "install:code": "npm install --prefix code"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "postcss": "^8.5.14",
    "tailwindcss": "^4.3.0"
  },
  "dependencies": {
    "framer-motion": "^12.38.0",
    "lucide-react": "^1.16.0"
  }
}
